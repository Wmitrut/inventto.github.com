<!DOCTYPE html>
<html>
<head>
<title>InventtoSite</title>
<%= stylesheet_link_tag    "application", :media => "all" %>
<%= javascript_include_tag "application" %>
<%= csrf_meta_tags %>
<%= javascript_include_tag :defaults %>

<script type="text/javascript">
  $(document).ready(function() {
      $("#projetos_accordion").accordion({
        collapsible: true, /* carrega todos os seletores fechados juntamente com o active e o alwaysOpen = false */
        active: false,
        alwaysOpen: false,
        animated: 'bounceslide',
        heightStyle: "content",
        event: 'hoverintent' /* or mouseover */
      });
  });
  var cfg = ($.hoverintent = {
    sensitivity: 6,
    interval: 200
  });

  $.event.special.hoverintent = {
    setup: function() {
      $( this ).bind( "mouseover", jQuery.event.special.hoverintent.handler );
    },
    teardown: function() {
      $( this ).unbind( "mouseover", jQuery.event.special.hoverintent.handler );
    },
    handler: function( event ) {
      var that = this,
        args = arguments,
        target = $( event.target ),
        cX, cY, pX, pY;

      function track( event ) {
        cX = event.pageX;
        cY = event.pageY;
      };
      pX = event.pageX;
      pY = event.pageY;
      function clear() {
        target
          .unbind( "mousemove", track )
          .unbind( "mouseout", arguments.callee );
        clearTimeout( timeout );
      }
      function handler() {
        if ( ( Math.abs( pX - cX ) + Math.abs( pY - cY ) ) < cfg.sensitivity ) {
          clear();
          event.type = "hoverintent";
          // prevent accessing the original event since the new event
          // is fired asynchronously and the old event is no longer
          // usable (#6028)
          event.originalEvent = {};
          jQuery.event.handle.apply( that, args );
        } else {
          pX = cX;
          pY = cY;
          timeout = setTimeout( handler, cfg.interval );
        }
      }
      var timeout = setTimeout( handler, cfg.interval );
      target.mousemove( track ).mouseout( clear );
      return true;
    }
  };
</script>

</head>
<body>
<div id="head">
  <div class="fix_width">
    <%= render :partial => "menu" %>
    <div id="logo">
      <div class="login">
        Bem Vindo <%= (n=session[:user_name]) ? ", " + n.to_s : "a Invent.to" %>
      <img class="login_img" src="<%= session[:user_picture] %>" />
      </div>
      <div class="inventto"> <a href="/">invent</a>.to </div>
      <span class="descricao_logo">Soluções em sistemas computacionais</span>
    </div>
  </div>
</div>
<%= flash_message %>
<div id="body" class="fix_width">
  <div class="slide_show"></div>
  <%= yield %>
  <i></i>
</div>
<div class="container">
  <div class="fix_width">
    <div class="side">
      <div class="title">Projetos Atuais</div>
      <div id="projetos_accordion">
        <% @todos_projetos.each do |projeto| %>
          <div class="projeto"><%= projeto.nome.upcase %></div>
          <div class="descricao"><%= projeto.descricao %></div>
        <% end %>
      </div>
    </div>
    <div class="side">
      <div class="title">Agenda</div>
      <iframe class="calendario" src="https://www.google.com/calendar/embed?showTitle=0&amp;showPrint=0&amp;showTabs=0&amp;showCalendars=0&amp;showTz=0&amp;height=210&amp;wkst=1&amp;bgcolor=%23999999&amp;src=invent.to.sistemas%40gmail.com&amp;color=%232F6309&amp;ctz=America%2FSao_Paulo" style=" border-width:0 " width="275" height="210" frameborder="0" scrolling="no"></iframe>
    </div>
    <div class="side">
      <div class="title">Contatos</div>
      Comercial:<span> (46) 3055-2138</span><br />
      Marlon:<span> (46) 8806-8254 - Claro</span><br />
      Marlon:<span> (41) 9677-4221 - Tim</span><br />
      Jônatas:<span> (46) 9911-7879 - Tim</span><br /> <br />
      email:<span> <a href="mailto:marlonscalabrin@gmail.com">marlonscalabrin@gmail.com</a></span><br />
      email:<span> <a href="mailto:jonatasdp@gmail.com">jonatasdp@gmail.com</a></span><br />
      <hr/>
      <a href="https://www.facebook.com/pages/Inventto/100538040118577?fref=ts" target="_blank"><img class="social" src="http://static.wix.com/media/da00086a27cc2c52ec7a11ec468c4d29.wix_mp_srz_28_28_85_22_0.50_1.20_0.00_wix_mp_srz"/></a>
      <a href="https://twitter.com/Invent_to" target="_blank"><img class="social" src="http://static.wix.com/media/4941bced49cf4de3e8d7ea1582892fb8.wix_mp_srz_28_28_85_22_0.50_1.20_0.00_wix_mp_srz"/></a>
      <a href="https://plus.google.com/u/0/100840270791572005293/posts"target="_blank"><img class="social" src="http://static.wix.com/media/608e7725939b0eda16493c462180552c.wix_mp_srz_28_28_85_22_0.50_1.20_0.00_wix_mp_srz"/></a>
    </div>
    <i></i>
  </div>
  <div class="extended">
    <div class="fix_width">
      <div id="copyright">Copyright 2013 "Invent.to"</div>
    <%= render :partial => "menu" %>
    </div>
  </div>
</div>
</body>
</html>
